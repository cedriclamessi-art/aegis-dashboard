{
  "version": 2,
  "buildCommand": "npm run build:api && npm run build:dashboard",
  "installCommand": "npm install && npm install --prefix frontend",
  "devCommand": "npm run dev",
  "outputDirectory": "frontend/dist"
}
